{
  "name": "ecom-suplementos-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "turbo run dev --filter=web",
    "dev:api": "turbo run start:dev --filter=api",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "prisma": "^7.6.0",
    "turbo": "^1.13.0"
  },
  "dependencies": {
    "@monorepo/shared": "^1.0.0",
    "@prisma/client": "^7.6.0",
    "zod": "^4.3.6"
  }
}
